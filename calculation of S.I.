#include<stdio.h>
int main()
{
  int p,n;      /* p is principle and t is time*/
  float r,si;   /* r is rate and si is simple intrest*/
 p=1000;
 n=3;
 r=8.5;
 si=(p*n*r)/100;
  printf(%f\n",si);
         }
